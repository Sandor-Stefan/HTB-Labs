
- Task 1:
	- The TCP open port on the machine is `6379`

- Task 2:
	- The service running on the port `6379` is `redis`

- Task 3:
	- `Redis` is an `In-memory Database`

- Task 4:
	- To interact with a redis server we use `redis-cli`

- Task 5:
	- To specify the hostname with the command `redis-cli` the `-h` options is used

- Task 6:
	- To obtain info and statistics about the Redis server we use the command `info`

- Task 7:
	- The Redis server version on the machine is `5.0.7`

- Task 8:
	- To select the desired database in Redis we use `select`

- Task 9:
	- There are `4` keys in the database 0

- Task 10:
	- To get all the keys in a database we use `keys *`

- To get the content of a key we use `get <key>`